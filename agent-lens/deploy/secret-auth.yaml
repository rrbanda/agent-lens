# Placeholder only — do NOT apply with REPLACE_ values.
# Prefer: make secret   (from repo root) which upserts real keys.
# Or:
#   oc create secret generic agent-lens-auth \
#     --from-literal=api-server-key='YOUR_SECURE_API_KEY' \
#     --from-literal=dashboard-password='YOUR_DASHBOARD_PASSWORD' \
#     -n agent-lens
apiVersion: v1
kind: Secret
metadata:
  name: agent-lens-auth
  labels:
    app.kubernetes.io/part-of: agent-lens
type: Opaque
stringData:
  api-server-key: "REPLACE_ME"
  dashboard-password: "REPLACE_ME"
