# Create this secret manually — do NOT commit real keys
# oc create secret generic agent-lens-llm-key \
#   --from-literal=api-key='YOUR_GEMINI_API_KEY' \
#   -n agent-lens
apiVersion: v1
kind: Secret
metadata:
  name: agent-lens-llm-key
  labels:
    app.kubernetes.io/part-of: agent-lens
type: Opaque
data:
  api-key: REPLACE_WITH_BASE64_ENCODED_KEY
