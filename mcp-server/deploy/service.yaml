apiVersion: v1
kind: Service
metadata:
  name: mlflow-mcp-server
  labels:
    app: mlflow-mcp-server
    app.kubernetes.io/part-of: agent-lens
spec:
  selector:
    app: mlflow-mcp-server
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: mcp
  type: ClusterIP
