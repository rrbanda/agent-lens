apiVersion: v1
kind: Service
metadata:
  name: agent-lens
  labels:
    app: agent-lens
    app.kubernetes.io/part-of: agent-lens
spec:
  selector:
    app: agent-lens
  ports:
    - port: 18789
      targetPort: 18789
      protocol: TCP
      name: dashboard
  type: ClusterIP
