# Create this secret manually — do NOT commit real credentials
# oc create secret generic agent-lens-auth \
#   --from-literal=api-server-key='YOUR_SECURE_API_KEY' \
#   --from-literal=dashboard-password='YOUR_DASHBOARD_PASSWORD' \
#   -n agent-lens
apiVersion: v1
kind: Secret
metadata:
  name: agent-lens-auth
  labels:
    app.kubernetes.io/part-of: agent-lens
type: Opaque
data:
  api-server-key: REPLACE_WITH_BASE64_ENCODED_KEY
  dashboard-password: REPLACE_WITH_BASE64_ENCODED_PASSWORD
